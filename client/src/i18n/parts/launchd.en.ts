/**
 * launchd domain vocabulary: key titles/help, trigger and calendar phrases, validation issues,
 * exit status text, job scopes and templates. English values are copied verbatim from
 * `shared/launchd.ts` (`LAUNCHD_KEYS`, `KEEPALIVE_CONDITIONS`, `CALENDAR_FIELDS`, `validateJob`,
 * `explainExitStatus`, `JOB_SCOPES`, `JOB_TEMPLATES`) so the English UI is byte-identical to the
 * shared/server output. See `client/src/i18n/launchd.ts` for how these are looked up.
 */
export const enLaunchd = {
  // ── Key titles ───────────────────────────────────────────────────
  "launchd.key.Label.title": "Label",
  "launchd.key.Label.help": "Unique job identifier. The plist file must be named <Label>.plist.",
  "launchd.key.Disabled.title": "Disabled",
  "launchd.key.Disabled.help": "Hint that the job should not be loaded. The launchctl override database takes precedence.",
  "launchd.key.UserName.title": "Run as user",
  "launchd.key.UserName.help": "User to run the job as. Only honoured for daemons.",
  "launchd.key.GroupName.title": "Run as group",
  "launchd.key.GroupName.help": "Group to run the job as. Only honoured for daemons.",
  "launchd.key.InitGroups.title": "Init groups",
  "launchd.key.InitGroups.help": "Call initgroups(3) before running the job.",
  "launchd.key.LimitLoadToSessionType.title": "Session types",
  "launchd.key.LimitLoadToSessionType.help": "Sessions the agent loads in.",
  "launchd.key.AssociatedBundleIdentifiers.title": "Associated bundle IDs",
  "launchd.key.AssociatedBundleIdentifiers.help": "Apps this job belongs to. macOS shows the app name under Login Items.",

  "launchd.key.Program.title": "Program",
  "launchd.key.Program.help": "Absolute path of the executable. When absent, the first ProgramArguments entry is used.",
  "launchd.key.ProgramArguments.title": "Program arguments",
  "launchd.key.ProgramArguments.help": "argv of the job. The first entry is the executable unless Program is set.",
  "launchd.key.BundleProgram.title": "Bundle program",
  "launchd.key.BundleProgram.help": "Executable path relative to the app bundle that registered the job.",
  "launchd.key.SpawnConstraint.title": "Spawn constraint",
  "launchd.key.SpawnConstraint.help": "Launch constraint the executable must satisfy (macOS 13.3+).",
  "launchd.key.EnableGlobbing.title": "Enable globbing",
  "launchd.key.EnableGlobbing.help": "Expand wildcards in ProgramArguments with glob(3).",

  "launchd.key.RunAtLoad.title": "Run at load",
  "launchd.key.RunAtLoad.help": "Start the job when it is loaded: at login for agents, at boot for daemons.",
  "launchd.key.KeepAlive.title": "Keep alive",
  "launchd.key.KeepAlive.help": "Keep the job running. Restart it unconditionally, or only under the chosen conditions.",
  "launchd.key.StartInterval.title": "Start interval",
  "launchd.key.StartInterval.help": "Start the job every N seconds.",
  "launchd.key.StartCalendarInterval.title": "Calendar schedule",
  "launchd.key.StartCalendarInterval.help": "Start the job at calendar times, like cron. Missing fields are wildcards.",
  "launchd.key.WatchPaths.title": "Watch paths",
  "launchd.key.WatchPaths.help": "Start the job when any of these files or folders is modified.",
  "launchd.key.QueueDirectories.title": "Queue directories",
  "launchd.key.QueueDirectories.help": "Keep the job alive while any of these directories is not empty.",
  "launchd.key.StartOnMount.title": "Start on mount",
  "launchd.key.StartOnMount.help": "Start the job every time a filesystem is mounted.",
  "launchd.key.LaunchOnlyOnce.title": "Launch only once",
  "launchd.key.LaunchOnlyOnce.help": "Run the job once per boot and never restart it.",
  "launchd.key.ThrottleInterval.title": "Throttle interval",
  "launchd.key.ThrottleInterval.help": "Minimum seconds between two starts. Default is 10.",
  "launchd.key.LaunchEvents.title": "Launch events",
  "launchd.key.LaunchEvents.help": "Start the job on higher-level events (IOKit matching, notifications).",
  "launchd.key.OnDemand.title": "On demand",
  "launchd.key.OnDemand.help": "Replaced by KeepAlive in macOS 10.5.",

  "launchd.key.EnvironmentVariables.title": "Environment variables",
  "launchd.key.EnvironmentVariables.help": "Extra environment variables for the job.",
  "launchd.key.WorkingDirectory.title": "Working directory",
  "launchd.key.WorkingDirectory.help": "chdir(2) to this directory before running the job.",
  "launchd.key.RootDirectory.title": "Root directory",
  "launchd.key.RootDirectory.help": "chroot(2) to this directory before running the job.",
  "launchd.key.Umask.title": "Umask",
  "launchd.key.Umask.help": "umask(2) value, as a decimal integer.",

  "launchd.key.StandardOutPath.title": "Standard output",
  "launchd.key.StandardOutPath.help": "File that receives stdout.",
  "launchd.key.StandardErrorPath.title": "Standard error",
  "launchd.key.StandardErrorPath.help": "File that receives stderr.",
  "launchd.key.StandardInPath.title": "Standard input",
  "launchd.key.StandardInPath.help": "File connected to stdin.",
  "launchd.key.Debug.title": "Debug",
  "launchd.key.Debug.help": "Raise launchd's log level while this job runs.",

  "launchd.key.ProcessType.title": "Process type",
  "launchd.key.ProcessType.help": "Resource policy applied to the job.",
  "launchd.key.Nice.title": "Nice",
  "launchd.key.Nice.help": "Scheduling priority from -20 (highest) to 20 (lowest).",
  "launchd.key.LowPriorityIO.title": "Low priority I/O",
  "launchd.key.LowPriorityIO.help": "Treat the job's filesystem I/O as low priority.",
  "launchd.key.LowPriorityBackgroundIO.title": "Low priority background I/O",
  "launchd.key.LowPriorityBackgroundIO.help": "Low priority I/O while the job is throttled to the background.",
  "launchd.key.SoftResourceLimits.title": "Soft resource limits",
  "launchd.key.SoftResourceLimits.help": "setrlimit(2) soft limits.",
  "launchd.key.HardResourceLimits.title": "Hard resource limits",
  "launchd.key.HardResourceLimits.help": "setrlimit(2) hard limits.",
  "launchd.key.ExitTimeOut.title": "Exit timeout",
  "launchd.key.ExitTimeOut.help": "Seconds between SIGTERM and SIGKILL when the job is stopped. Default is 20.",
  "launchd.key.TimeOut.title": "Timeout",
  "launchd.key.TimeOut.help": "Recommended inactivity timeout handed to the job.",

  "launchd.key.AbandonProcessGroup.title": "Abandon process group",
  "launchd.key.AbandonProcessGroup.help": "Do not kill the remaining processes of the job's process group when the job exits.",
  "launchd.key.EnableTransactions.title": "Enable transactions",
  "launchd.key.EnableTransactions.help": "The job uses XPC transactions to signal outstanding work.",
  "launchd.key.EnablePressuredExit.title": "Enable pressured exit",
  "launchd.key.EnablePressuredExit.help": "launchd can stop the job under memory pressure when it is clean.",
  "launchd.key.SessionCreate.title": "Session create",
  "launchd.key.SessionCreate.help": "Run the job in a new security audit session.",
  "launchd.key.LegacyTimers.title": "Legacy timers",
  "launchd.key.LegacyTimers.help": "Opt out of timer coalescing.",
  "launchd.key.MaterializeDatalessFiles.title": "Materialize dataless files",
  "launchd.key.MaterializeDatalessFiles.help": "Allow the job to materialize dataless (cloud-only) files.",
  "launchd.key.WaitForDebugger.title": "Wait for debugger",
  "launchd.key.WaitForDebugger.help": "Start the job suspended until a debugger attaches.",
  "launchd.key.MachServices.title": "Mach services",
  "launchd.key.MachServices.help": "Mach service names the job registers with the bootstrap server.",
  "launchd.key.Sockets.title": "Sockets",
  "launchd.key.Sockets.help": "Launch-on-demand sockets.",
  "launchd.key.inetdCompatibility.title": "inetd compatibility",
  "launchd.key.inetdCompatibility.help": "The job expects inetd-style socket handling.",
  "launchd.key.LimitLoadToHosts.title": "Limit load to hosts",
  "launchd.key.LimitLoadToHosts.help": "No longer supported by launchd.",
  "launchd.key.LimitLoadFromHosts.title": "Limit load from hosts",
  "launchd.key.LimitLoadFromHosts.help": "No longer supported by launchd.",
  "launchd.key.LimitLoadToHardware.title": "Limit load to hardware",
  "launchd.key.LimitLoadToHardware.help": "Load only on matching hardware (sysctl values).",
  "launchd.key.LimitLoadFromHardware.title": "Limit load from hardware",
  "launchd.key.LimitLoadFromHardware.help": "Do not load on matching hardware (sysctl values).",
  "launchd.key.HopefullyExitsFirst.title": "Hopefully exits first",
  "launchd.key.HopefullyExitsFirst.help": "No longer supported by launchd.",
  "launchd.key.HopefullyExitsLast.title": "Hopefully exits last",
  "launchd.key.HopefullyExitsLast.help": "No longer supported by launchd.",

  // ── Keep-alive conditions ────────────────────────────────────────
  "launchd.keepalive.SuccessfulExit.help": "true: restart until the job fails. false: restart until it exits with 0.",
  "launchd.keepalive.Crashed.help": "true: restart only after a crash. false: restart only after a clean exit.",
  "launchd.keepalive.PathState.help": "Keep alive while a path exists (true) or does not exist (false).",
  "launchd.keepalive.OtherJobEnabled.help": "Keep alive while another job is enabled (true) or disabled (false).",
  "launchd.keepalive.AfterInitialDemand.help": "Apply the other conditions only after the job has been started once on demand.",
  "launchd.keepalive.NetworkState.help": "No longer implemented by launchd.",

  // ── Calendar field ranges ────────────────────────────────────────
  "launchd.calendar.Month.help": "1–12",
  "launchd.calendar.Day.help": "1–31",
  "launchd.calendar.Weekday.help": "0 and 7 are Sunday",
  "launchd.calendar.Hour.help": "0–23",
  "launchd.calendar.Minute.help": "0–59",

  // ── Calendar phrase fragments (see describeCalendarEntry) ───────
  "launchd.calendar.atTime": "at {hour}:{minute}",
  "launchd.calendar.everyMinuteOfHour": "every minute of hour {hour}",
  "launchd.calendar.atMinuteOfEveryHour": "at minute {minute} of every hour",
  "launchd.calendar.everyMinute": "every minute",
  "launchd.calendar.onDay": "on day {day}",
  "launchd.calendar.onWeekday": "on {weekday}",
  "launchd.calendar.or": "or",
  "launchd.calendar.inMonth": "in {month}",

  // ── Trigger tag vocabulary (see describeTriggers) ────────────────
  "launchd.trigger.atLoad": "At load",
  "launchd.trigger.keepAlive": "Keep alive",
  "launchd.trigger.keepAliveConditional": "Keep alive (conditional)",
  "launchd.trigger.every": "Every {interval}",
  "launchd.trigger.calendar": "Calendar: {entry}",
  "launchd.trigger.calendarCount": "Calendar ×{count}",
  "launchd.trigger.watchesPath.one": "Watches {count} path",
  "launchd.trigger.watchesPath.other": "Watches {count} paths",
  "launchd.trigger.queueDirectory": "Queue directory",
  "launchd.trigger.onMount": "On mount",
  "launchd.trigger.socket": "Socket",
  "launchd.trigger.machService": "Mach service",
  "launchd.trigger.launchEvent": "Launch event",

  // ── Interval units (see formatInterval) ──────────────────────────
  "launchd.unit.s": "s",
  "launchd.unit.min": "min",
  "launchd.unit.h": "h",
  "launchd.unit.d": "d",

  // ── Validation issues (see validateJob) ──────────────────────────
  "launchd.issue.label_required": "Label is required.",
  "launchd.issue.label_pattern": "Label may only contain letters, digits, dots, dashes and underscores.",
  "launchd.issue.file_name_mismatch": 'File name "{fileName}" does not match the label. launchd convention is "{label}.plist".',
  "launchd.issue.label_duplicate": "Another job in this scope already uses this label.",
  "launchd.issue.label_apple_prefix": 'The "com.apple." prefix is reserved for macOS jobs.',
  "launchd.issue.nothing_to_run": "The job has nothing to run. Set Program or ProgramArguments.",
  "launchd.issue.program_arguments_empty": "ProgramArguments is empty.",
  "launchd.issue.tilde_not_expanded": "launchd does not expand ~. Use the full path.",
  "launchd.issue.program_not_absolute": '"{exe}" is not an absolute path. launchd searches a minimal PATH (/usr/bin:/bin:/usr/sbin:/sbin).',
  "launchd.issue.executable_missing": "Executable not found: {exe}",
  "launchd.issue.executable_is_app": "{exe} is an app bundle. Run it with /usr/bin/open -a, or point to Contents/MacOS/<binary>.",
  "launchd.issue.executable_is_directory": "{exe} is a directory.",
  "launchd.issue.executable_not_executable": "File is not executable: {exe} (chmod +x).",
  "launchd.issue.no_trigger": "No trigger is set. The job only runs when you start it manually.",
  "launchd.issue.start_interval_min": "StartInterval must be at least 1 second.",
  "launchd.issue.start_interval_keepalive_noop": "StartInterval has no effect while KeepAlive keeps the job running.",
  "launchd.issue.start_interval_throttled": "StartInterval is shorter than ThrottleInterval, so launchd delays the starts.",
  "launchd.issue.calendar_unknown_field": 'Schedule {index}: unknown field "{field}".',
  "launchd.issue.calendar_field_range": "Schedule {index}: {field} must be an integer between {min} and {max}.",
  "launchd.issue.keepalive_network_state_unsupported": "KeepAlive.NetworkState is no longer implemented by launchd.",
  "launchd.issue.path_missing": "Path does not exist yet: {path}",
  "launchd.issue.queue_directory_not_a_directory": "Not a directory: {path}",
  "launchd.issue.working_directory_missing": "Working directory not found: {path}",
  "launchd.issue.log_folder_missing": "Folder does not exist: {path}. launchd does not create it.",
  "launchd.issue.key_ignored_for_agents": "{key} is ignored for agents. It only applies to daemons.",
  "launchd.issue.session_type_ignored_for_daemons": "LimitLoadToSessionType applies to agents, not daemons.",
  "launchd.issue.nice_range": "Nice must be between -20 and 20.",
  "launchd.issue.process_type_invalid": "ProcessType must be Background, Standard, Adaptive or Interactive.",
  "launchd.issue.key_undocumented": '"{key}" is not a documented launchd key. launchd ignores unknown keys.',
  "launchd.issue.key_wrong_type": "{key} has the wrong type. Expected {type}.",
  "launchd.issue.key_deprecated": "{key} is deprecated. {help}",

  // ── Exit status (see explainExitStatus) ──────────────────────────
  "launchd.exit.0": "Success",
  "launchd.exit.1": "General error",
  "launchd.exit.2": "Misuse of a shell builtin or wrong arguments",
  "launchd.exit.64": "Command line usage error (EX_USAGE)",
  "launchd.exit.66": "Cannot open input (EX_NOINPUT)",
  "launchd.exit.69": "Service unavailable (EX_UNAVAILABLE)",
  "launchd.exit.70": "Internal software error (EX_SOFTWARE)",
  "launchd.exit.73": "Cannot create output file (EX_CANTCREAT)",
  "launchd.exit.74": "I/O error (EX_IOERR)",
  "launchd.exit.77": "Permission denied (EX_NOPERM)",
  "launchd.exit.78":
    "Configuration error (EX_CONFIG). launchd reports 78 when it cannot start the program: check the path, the permissions and the log paths.",
  "launchd.exit.126": "Command found but not executable",
  "launchd.exit.127": "Command not found",
  "launchd.exit.signalNamed": "Terminated by signal {number} ({name})",
  "launchd.exit.signal": "Terminated by signal {number}",
  "launchd.exit.unknown": "Exited with code {code}",

  // ── Job scopes (see JOB_SCOPES) ───────────────────────────────────
  "launchd.scope.user-agents.title": "User Agents",
  "launchd.scope.user-agents.description": "Runs as you, while you are logged in.",
  "launchd.scope.global-agents.title": "Global Agents",
  "launchd.scope.global-agents.description": "Runs for every user who logs in, as that user.",
  "launchd.scope.global-daemons.title": "Global Daemons",
  "launchd.scope.global-daemons.description": "Runs as root (or UserName) from boot, with no user logged in.",
  "launchd.scope.system-agents.title": "System Agents",
  "launchd.scope.system-agents.description": "Part of macOS. Read-only.",
  "launchd.scope.system-daemons.title": "System Daemons",
  "launchd.scope.system-daemons.description": "Part of macOS. Read-only.",

  // ── Job templates (see JOB_TEMPLATES) ─────────────────────────────
  "launchd.template.blank.title": "Blank job",
  "launchd.template.blank.description": "Only a label and a command.",
  "launchd.template.login.title": "Run at login",
  "launchd.template.login.description": "Starts an app or script every time you log in.",
  "launchd.template.interval.title": "Run every N minutes",
  "launchd.template.interval.description": "Runs a command on a fixed interval.",
  "launchd.template.calendar.title": "Run at a specific time",
  "launchd.template.calendar.description": "Runs a command every day at 09:00, like cron.",
  "launchd.template.keepalive.title": "Keep a program running",
  "launchd.template.keepalive.description": "Restarts the program whenever it quits or crashes.",
  "launchd.template.watch.title": "Run when a file or folder changes",
  "launchd.template.watch.description": "Starts the command when a watched path is modified.",
  "launchd.template.mount.title": "Run when a volume is mounted",
  "launchd.template.mount.description": "Starts the command every time a disk is mounted.",
} as const;
